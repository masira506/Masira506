<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Floral Portfolio</title>
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Playfair Display', serif;
            background: linear-gradient(135deg, #fff5e6 0%, #ffe6f2 100%);
            color: #5a3e36;
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* Header and Navigation */
        header {
            background: rgba(255, 245, 230, 0.9);
            padding: 20px;
            position: fixed;
            width: 100%;
            z-index: 1000;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            animation: slideDown 1s ease;
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1200px;
            margin: 0 auto;
        }

        .logo {
            font-family: 'Dancing Script', cursive;
            font-size: 2.5rem;
            font-weight: bold;
            color: #d46a92;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
            animation: pulse 2s infinite;
        }

        .nav-links {
            display: flex;
            list-style: none;
        }

        .nav-links li {
            margin-left: 30px;
        }

        .nav-links a {
            text-decoration: none;
            color: #5a3e36;
            font-weight: bold;
            font-size: 1.1rem;
            transition: all 0.3s ease;
            position: relative;
        }

        .nav-links a:hover {
            color: #d46a92;
        }

        .nav-links a::after {
            content: '';
            position: absolute;
            width: 0;
            height: 2px;
            bottom: -5px;
            left: 0;
            background-color: #d46a92;
            transition: width 0.3s ease;
        }

        .nav-links a:hover::after {
            width: 100%;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            padding: 100px 5% 50px;
            text-align: center;
            overflow: hidden;
        }

        .hero-content {
            max-width: 800px;
            z-index: 2;
        }

        .hero h1 {
            font-family: 'Dancing Script', cursive;
            font-size: 5rem;
            color: #d46a92;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.2);
            animation: fadeIn 2s ease;
        }

        .hero p {
            font-size: 1.5rem;
            margin-bottom: 30px;
            animation: fadeIn 2.5s ease;
        }

        .btn {
            display: inline-block;
            padding: 15px 30px;
            background: linear-gradient(45deg, #d46a92, #ff9bbf);
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            box-shadow: 0 5px 15px rgba(212, 106, 146, 0.4);
            animation: pulse 2s infinite 3s;
        }

        .btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(212, 106, 146, 0.6);
        }

        /* About Section */
        .about {
            padding: 100px 5%;
            text-align: center;
            background: rgba(255, 255, 255, 0.8);
        }

        .section-title {
            font-family: 'Dancing Script', cursive;
            font-size: 3.5rem;
            color: #d46a92;
            margin-bottom: 50px;
            position: relative;
            display: inline-block;
        }

        .section-title::after {
            content: '';
            position: absolute;
            width: 100px;
            height: 3px;
            background: linear-gradient(45deg, #d46a92, #ff9bbf);
            bottom: -15px;
            left: 50%;
            transform: translateX(-50%);
        }

        .about-content {
            max-width: 1000px;
            margin: 0 auto;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
        }

        .about-text {
            flex: 1;
            min-width: 300px;
            padding: 20px;
            text-align: left;
        }

        .about-text h3 {
            font-size: 2rem;
            margin-bottom: 20px;
            color: #5a3e36;
        }

        .about-image {
            flex: 1;
            min-width: 300px;
            padding: 20px;
        }

        .profile-img {
            width: 100%;
            max-width: 400px;
            height: auto;
            border-radius: 50%;
            border: 10px solid #ffe6f2;
            box-shadow: 0 0 25px rgba(212, 106, 146, 0.5);
            animation: float 6s ease-in-out infinite;
        }

        /* Portfolio Section */
        .portfolio {
            padding: 100px 5%;
            background: linear-gradient(135deg, #ffe6f2 0%, #fff5e6 100%);
        }

        .projects {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 30px;
            margin-top: 50px;
        }

        .project-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            width: 300px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            animation: fadeIn 2s ease;
        }

        .project-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
        }

        .project-img {
            width: 100%;
            height: 200px;
            background: linear-gradient(45deg, #d46a92, #ff9bbf);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 3rem;
        }

        .project-info {
            padding: 20px;
        }

        .project-info h3 {
            font-size: 1.5rem;
            margin-bottom: 10px;
            color: #d46a92;
        }

        /* Contact Section */
        .contact {
            padding: 100px 5%;
            text-align: center;
            background: rgba(255, 255, 255, 0.8);
        }

        .contact-form {
            max-width: 600px;
            margin: 50px auto;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .form-group {
            display: flex;
            gap: 20px;
        }

        input, textarea {
            padding: 15px;
            border: 2px solid #ffe6f2;
            border-radius: 10px;
            font-size: 1rem;
            font-family: 'Playfair Display', serif;
            width: 100%;
            transition: all 0.3s ease;
        }

        input:focus, textarea:focus {
            outline: none;
            border-color: #d46a92;
            box-shadow: 0 0 10px rgba(212, 106, 146, 0.3);
        }

        textarea {
            height: 150px;
            resize: vertical;
        }

        /* Footer */
        footer {
            background: #5a3e36;
            color: white;
            padding: 30px 5%;
            text-align: center;
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin: 20px 0;
        }

        .social-links a {
            color: white;
            font-size: 1.5rem;
            transition: all 0.3s ease;
        }

        .social-links a:hover {
            color: #ff9bbf;
            transform: translateY(-5px);
        }

        /* Floral Elements and Animations */
        .floral-element {
            position: absolute;
            font-size: 2rem;
            color: #d46a92;
            opacity: 0.7;
            z-index: 1;
        }

        .floral-1 {
            top: 10%;
            left: 5%;
            animation: float 6s ease-in-out infinite;
        }

        .floral-2 {
            top: 20%;
            right: 10%;
            animation: float 7s ease-in-out infinite 1s;
        }

        .floral-3 {
            bottom: 30%;
            left: 15%;
            animation: float 8s ease-in-out infinite 2s;
        }

        .floral-4 {
            bottom: 20%;
            right: 5%;
            animation: float 9s ease-in-out infinite 3s;
        }

        /* Animations */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.05); }
            100% { transform: scale(1); }
        }

        @keyframes float {
            0% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(5deg); }
            100% { transform: translateY(0) rotate(0deg); }
        }

        @keyframes slideDown {
            from { transform: translateY(-100px); }
            to { transform: translateY(0); }
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 3.5rem;
            }
            
            .nav-links {
                display: none;
            }
            
            .form-group {
                flex-direction: column;
                gap: 10px;
            }
        }
    </style>
</head>
<body>
    <!-- Header with Navigation -->
    <header>
        <nav>
            <div class="logo">Floral Portfolio</div>
            <ul class="nav-links">
                <li><a href="#home">Home</a></li>
                <li><a href="#about">About</a></li>
                <li><a href="#portfolio">Portfolio</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero" id="home">
        <div class="floral-element floral-1">🌸</div>
        <div class="floral-element floral-2">🌺</div>
        <div class="floral-element floral-3">🌼</div>
        <div class="floral-element floral-4">🌷</div>
        
        <div class="hero-content">
            <h1>Hello, I'm Emily</h1>
            <p>A Creative Designer & Web Developer</p>
            <a href="#portfolio" class="btn">View My Work</a>
        </div>
    </section>

    <!-- About Section -->
    <section class="about" id="about">
        <h2 class="section-title">About Me</h2>
        <div class="about-content">
            <div class="about-text">
                <h3>Designing with flowers in mind</h3>
                <p>I'm a passionate designer and developer who loves to create beautiful, floral-inspired digital experiences. With over 5 years of experience in the industry, I specialize in creating visually stunning websites and applications that leave a lasting impression.</p>
                <p>My design philosophy is inspired by nature's beauty - combining organic shapes, soft colors, and elegant typography to create harmonious digital spaces.</p>
                <p>When I'm not designing, you can find me gardening, practicing floral arrangement, or exploring new botanical gardens for inspiration.</p>
            </div>
            <div class="about-image">
                <div class="profile-img">🌻</div>
            </div>
        </div>
    </section>

    <!-- Portfolio Section -->
    <section class="portfolio" id="portfolio">
        <h2 class="section-title">My Portfolio</h2>
        <div class="projects">
            <div class="project-card">
                <div class="project-img">🌹</div>
                <div class="project-info">
                    <h3>Floral E-commerce</h3>
                    <p>An online flower shop with interactive bouquet customizer and seamless checkout experience.</p>
                </div>
            </div>
            <div class="project-card">
                <div class="project-img">🌺</div>
                <div class="project-info">
                    <h3>Botanical Blog</h3>
                    <p>A nature-inspired blog platform with custom floral themes and engaging user experience.</p>
                </div>
            </div>
            <div class="project-card">
                <div class="project-img">🌸</div>
                <div class="project-info">
                    <h3>Garden Design App</h3>
                    <p>An application that helps users plan and visualize their dream gardens with AR technology.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="contact" id="contact">
        <h2 class="section-title">Get In Touch</h2>
        <div class="contact-form">
            <div class="form-group">
                <input type="text" placeholder="Your Name">
                <input type="email" placeholder="Your Email">
            </div>
            <input type="text" placeholder="Subject">
            <textarea placeholder="Your Message"></textarea>
            <button class="btn">Send Message</button>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="social-links">
            <a href="#"><i class="fab fa-instagram"></i></a>
            <a href="#"><i class="fab fa-twitter"></i></a>
            <a href="#"><i class="fab fa-linkedin-in"></i></a>
            <a href="#"><i class="fab fa-behance"></i></a>
        </div>
        <p>&copy; 2023 Floral Portfolio. All rights reserved.</p>
    </footer>

    <script>
        // Simple animations for page elements
        document.addEventListener('DOMContentLoaded', function() {
            // Animate project cards on scroll
            const projectCards = document.querySelectorAll('.project-card');
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.animation = 'fadeIn 1.5s ease forwards';
                        observer.unobserve(entry.target);
                    }
                });
            }, { threshold: 0.1 });
            
            projectCards.forEach(card => {
                card.style.opacity = '0';
                observer.observe(card);
            });
            
            // Smooth scrolling for navigation links
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function(e) {
                    e.preventDefault();
                    
                    const targetId = this.getAttribute('href');
                    const targetElement = document.querySelector(targetId);
                    
                    if (targetElement) {
                        window.scrollTo({
                            top: targetElement.offsetTop - 80,
                            behavior: 'smooth'
                        });
                    }
                });
            });
        });
    </script>
</body>
</html>
